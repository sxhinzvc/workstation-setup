#!/bin/bash

# git
alias gst='git status'

#
alias ll='ls -la'

# bash
alias hist='history'
alias cls='clear'

# terraform
alias tf='terraform'

# docker
alias dckr='docker run -v $(pwd):/srv -w /srv -it'

# grep with color
alias grep='grep --color=auto'

# refresh shell
alias reload='source ~/.bash_profile'

# terraform
alias tf='terraform'

# kubectl
alias k8='kubectl'
